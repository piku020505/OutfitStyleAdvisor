# Optional: enables Gen AI (Claude) style-report generation.
# If unset, the app automatically falls back to a rule-based style report
# so the rest of the pipeline keeps working without an API key.
ANTHROPIC_API_KEY=your_api_key_here
